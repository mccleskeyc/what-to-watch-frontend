const initialState = {
    movies: [],
    loading: true
}
const moviesReducer = (state =initialState, action) => {
    switch(action.type) {
        default:
            return state;
    }
}

export default moviesReducer;